// script.js
document.addEventListener('DOMContentLoaded', function() {
    // Simulated data (you can replace this with actual data)
    const totalDonors = 100;
    const peopleHelped = 250;

    // Update the counters with the simulated data
    document.getElementById('donor-count').textContent = totalDonors;
    document.getElementById('people-helped-count').textContent = peopleHelped;
});
